# Keep the build context small and avoid copying host (Windows) build artifacts
# into the Linux image — Gradle rebuilds them inside the container.
.git/
.jj/
.gradle/
build/
.kotlin/

# IDE / local agent config — never needed for the build.
.idea/
*.iml
.claude/
CLAUDE.local.md
