package net.zelanton.processkit

/**
 * Returns a friendly greeting for [name].
 *
 * Placeholder API so the template compiles and tests pass out of the box.
 * Delete it and replace it with your library's real public surface.
 */
public fun greet(name: String): String = "Hello, $name!"
